import { StoryblokComponentDirective } from './storyblok-component.directive';

describe('StoryblokComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new StoryblokComponentDirective();
    expect(directive).toBeTruthy();
  });
});
