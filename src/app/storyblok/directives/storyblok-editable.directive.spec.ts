import { StoryblokEditableDirective } from './storyblok-editable.directive';

describe('StoryblokEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new StoryblokEditableDirective();
    expect(directive).toBeTruthy();
  });
});
